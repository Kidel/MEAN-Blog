version: '3'
services:
  rest:
    build: RestServer/.
    links:
    - database
    depends_on: 
    - database
    environment:
    - MONGO_URL=mongodb://database:27017/
  application:
    build: ApplicationServer/.
    ports:
    - "8080:8080"
    links:
    - rest
  database:
    image: mongo
